{
  "name": "@savecloud/crawler",
  "version": "26.9.1",
  "private": true,
  "description": "Modular web crawler and hoster resolution engine for SaveCloud",
  "scripts": {
    "test": "python -m unittest discover -s tests -p \"test_*.py\"",
    "check": "python -m compileall crawler"
  }
}
